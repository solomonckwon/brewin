Starting over from scratch using a new approach.

Reference args were not working correctly, so instead I will implement it within the environment class, instead as a value within a list of Type.value()