func main() {
  x = 0;
  lam1 = lambda(ref x) { x = x + 1; print(x); }; 
  lam2 = lambda(x) { x = x + 1; print(x); };
  lam1(x);
  lam2(x);
  print(x);
}

/*
*OUT*
1
2
1
*OUT*
*/

/* test_ref3 after this */