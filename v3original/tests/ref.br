func main() {
  x = 0;
  lam1 = lambda(ref x) { x = x + 1; print(x); };
  lam2 = lambda(ref x) { x = x + 1; print(x); };
  lam1(x);
  lam2(x);
  lam3 = lam2(x);
  lam3();
  print(x);
}

/*
*OUT*
1
2
2
*OUT*
*/